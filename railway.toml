[deploy]
startCommand = "gunicorn web_coach:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3