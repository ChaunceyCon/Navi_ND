# Web server
flask>=3.0.0
gunicorn>=22.0.0

# Anthropic SDK (direct API — more reliable than Agent SDK on hosted infra)
anthropic>=0.40.0

# Claude Agent SDK (used by coach.py CLI only — not the web server)
claude-agent-sdk>=0.1.0
